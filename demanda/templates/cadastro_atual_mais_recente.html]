<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro de Usuário</title>
    <style>
        /* Estilos gerais para o corpo da página */
        body {
            font-family: Arial, sans-serif; /* Fonte do texto */
            margin: 0; /* Remove margens padrão */
            padding: 0px 0 0; /* Adiciona um preenchimento superior de 20 pixels, mantendo 0 nos demais lados */
            box-sizing: border-box; /* Garante que o modelo de caixa seja de borda (border-box) */
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh; /* Pelo menos 100% da altura da viewport */
            background-color: #F5F5F5; /* Cor de fundo da página */
        }

        /* Estilo para o título do formulário */
        h2 {
            text-align: center;
            margin: 0px 0 10; /* Adiciona uma margem superior de 20 pixels, mantendo 0 nas laterais e na parte inferior */
        }
        

        /* Estilos para o formulário */
        form {
            width: 100%;
            max-width: 400px; /* Ajuste conforme necessário */
            padding: 20px;
            box-sizing: border-box;
            text-align: center;
            
        }

        /* Estilos para as etiquetas (labels) dos campos */
        label {
            display: block; /* Faz com que as etiquetas se comportem como blocos */
            margin-bottom: 4px; /* Adiciona espaço abaixo de cada etiqueta */
            text-align: left; /* Alinha o texto à esquerda */
        }

        /* Estilos para os campos de entrada (inputs) e botão */
        input,
        button {
            width: 100%; /* Ocupa a largura total do pai */
            padding: 8px; /* Adiciona preenchimento interno */
            margin-bottom: 8px; /* Adiciona espaço abaixo de cada input e botão */
            box-sizing: border-box; /* Garante que o modelo de caixa seja de borda (border-box) */
        }

        /* Estilos para o campo de entrada (input) */
        input {
            border: 1px solid #ccc; /* Cor da borda padrão */
            border-radius: 5px; /* Bordas arredondadas */
            transition: border-color 0.3s ease; /* Transição suave na cor da borda */
        }

        /* Estilos quando o campo de entrada (input) está em foco (selecionado) */
        input:focus {
            border-color: blue; /* Cor da borda quando o input é selecionado (focus) */
            box-shadow: 0 0 5px rgba(0, 0, 255, 0.5); /* Adiciona uma sombra azul suave quando o input está em foco */
            outline: none; /* Remove a borda padrão do navegador ao focar */
        }

        /* Estilos para o botão */
        button {
            width: 200px; /* Ou qualquer outro valor desejado */
            height: 40px; /* Ajuste conforme necessário para a altura */
            padding: 10px; /* Ajuste conforme necessário */
            background-color: blue;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 5px; /* Adiciona bordas arredondadas */
            transition: background-color 0.3s ease; /* Adiciona transição suave na mudança de cor de fundo */
            font-weight: bold; /* Torna o texto em negrito */
        }

        /* Estilos responsivos para telas maiores que 768 pixels */
        @media screen and (min-width: 768px) {
            form {
                width: 50%; /* Reduz a largura do formulário para telas maiores */
            }
        }
    </style>
</head>

<body>
    <!-- Título do formulário -->
    <h2>Cadastro de Usuário</h2>

    <!-- Formulário de cadastro -->
    <form method="post" action="{% url 'cadastro' %}">
        {% csrf_token %}
        
        <!-- Etiqueta e campo para o nome -->
        <label for="id_nome">Nome:</label>
        <input type="text" id="id_nome" name="nome" required>

        <!-- Etiqueta e campo para o email -->
        <label for="id_email">Email:</label>
        <input type="email" id="id_email" name="email" required>

        <!-- Etiqueta e campo para o telefone -->
        <label for="id_telefone">Telefone:</label>
        <input type="tel" id="id_telefone" name="telefone" required>

        <!-- Etiqueta e campo para a senha -->
        <label for="id_senha">Senha:</label>
        <input type="password" id="id_senha" name="senha" required>

        <!-- Etiqueta e campo para a confirmação de senha -->
        <label for="id_confirm_senha">Confirmação de Senha:</label>
        <input type="password" id="id_confirm_senha" name="confirm_senha" required>

        <!-- Botão de envio do formulário -->
        <br>
        <br>
        <button type="submit">Cadastrar</button>
    </form>
</body>

<!-- Script para formatar o número de telefone -->
<script>
    var telefone = document.getElementById("id_telefone");

    telefone.addEventListener("input", function () {
        var limparValor = telefone.value.replace(/\D/g, "").substring(0, 11);
        var numerosArray = limparValor.split("");
        var numeroFormatado = "";

        if (numerosArray.length > 0) {
            numeroFormatado += `(${numerosArray.slice(0, 2).join("")})`;
        }

        if (numerosArray.length > 2) {
            numeroFormatado += ` ${numerosArray.slice(2, 7).join("")}`;
        }

        if (numerosArray.length > 7) {
            numeroFormatado += `-${numerosArray.slice(7, 11).join("")}`;
        }

        telefone.value = numeroFormatado;
    });
</script>

</html>